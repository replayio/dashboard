# Replay Dashboard — Releases

**Route:** `/releases`

Lists Replay browser/runtime releases. Data is loaded from the public API (`/v1/releases` on the configured API host).

For machine-readable release data, prefer the JSON API over scraping this page.

- Docs: https://docs.replay.io
